<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <title>Edit Asset</title>
</head>
<body>
    <h1>Edit Asset</h1>
    <form action="/inventory/edit?_id=<%= Asset._id %>" method="POST">

        <input type="hidden" name="_id" value="<%= Asset._id %>">

        <label class="label" for="assetID">Asset ID</label>
        <input type="number" id="assetID" name="assetID" value="<%= Asset.assetID %>" required><br><br>

        <label class="label" for="assetName">Asset Name</label>
        <input type="text" id="assetName" name="assetName" value="<%= Asset.assetName %>" required><br><br>
  
        <div>
            <label class="label" for="assetType">Asset Type</label>
            <select id="assetType" name="assetType">
                <option value="Laptop">Laptop</option>
                <option value="Monitor">Monitor</option>
                <option value="PC">PC</option>
                <option value="Adapter">Adapter</option>
                <option value="Cable">Cable</option>
            </select>
        </div>
       
        <label class="label" for="OwnerID">Owner ID</label>
        <select id="OwnerID" name="OwnerID"></select>

        <input type="submit" value="Save">

    </form>

    <script>
  fetch('/inventory/users-data')  // Update the route based on your server setup
  .then(response => response.json())
  .then(users => {
    // Get the select element
    var selectElement = document.getElementById('OwnerID');

    // Loop through the users and create options
    users.forEach(user => {
      var option = document.createElement('option');
      option.value = user.userID; // Assuming userID is the value you want
      option.text = `${user.firstName} ${user.lastName}`; // Display name or other user info
      selectElement.appendChild(option);
    });
  })
  .catch(error => console.error('Error fetching user data:', error));




    </script>

    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
</body>
</html>
